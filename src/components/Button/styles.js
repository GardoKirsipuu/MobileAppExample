import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#4F63AC',
        padding: 8,
        borderRadius: 8,
        paddingVertical: 20,
        width: '100%',
        
    },
    title: {
        color: '#fff',
        fontSize: 16,
        fontWeight: 'bold',
        textAlign: 'center',
    }
})